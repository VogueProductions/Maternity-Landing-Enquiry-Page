Vogue Landing Page Setup

1. Upload all files in this folder to Firebase Hosting (public folder)
2. Ensure index.html is at root
3. Run:
   firebase init
   firebase deploy

No additional files required (all CSS & JS are inline)

WhatsApp link already configured.
